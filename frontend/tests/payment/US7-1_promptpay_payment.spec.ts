
// import { test, expect } from "@playwright/test";
// import TestPage from "../TestPage";

// test("has title", async ({ page }) => {
//   const app = new TestPage(page);
//   await app.goto("/payment");
  

//  const Room = page.locator("p", { hasText: "Room" }).first();
//  const Reservation = page.locator("p", { hasText: "Reservation" }).first();

// // // Then assert
// await expect(Room).toHaveText(/Room/);
// await expect(Reservation).toHaveText(/Reservation/);
// });

// await page.getByRole('link', { name: 'profile' }).click();